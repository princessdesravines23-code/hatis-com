import LegalPage from "../components/LegalPage";

export default function Privacy() {
  return (
    <LegalPage
      title="Privacy"
      body="Placeholder — real privacy policy goes here before launch. Hatis stores what a creator adds to their own profile (bio, socials, payment handles) and basic account info. We never collect ID documents or financial account access, since we never touch supporter payments directly."
    />
  );
}